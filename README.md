# Расписание 10-Л — СУНЦ МГУ

Химический класс, I семестр 2026/27. Кабинет 40, 38 уроков в неделю.

**Сайт:** https://s5hyhbpftv-alt.github.io/sunc-10l/
**Приложение:** https://s5hyhbpftv-alt.github.io/sunc-10l/app.html
(`?world=kraft` открывает крафтовый мир сразу)

## Что готово

| Файл | Что это |
|---|---|
| `out/Расписание 10-Л — СУНЦ МГУ.pdf` | **книга А4, 12 полос** — чернильное издание |
| `out/Расписание 10-Л — крафт.pdf` | то же на крафтовой бумаге, ризография |
| `out/Расписание 10-Л — таблица.pdf` | **вся неделя одной таблицей**, A4 поперёк, под чёрно-белый принтер |
| `out/v1-raspisanie-10L-SUNC-MGU.pdf` | первый вариант: A3, альбомный, 10 полос |
| `web/` | сайт на React с переключателем миров |
| `assets/sunc-cubes.svg` | эмблема СУНЦ МГУ, перерисованная вектором |

Полосы книг: обложка · ключ · хронометраж · шесть проб дня · хроматограмма ·
состав · выходные данные.

## Пересобрать

```bash
python3 книга.py        # чернильная книга А4
python3 книга-крафт.py  # крафтовая книга А4
python3 build_v1.py     # первый вариант, A3
python3 таблица.py      # лист-таблица для монохромной печати
python3 экспорт.py      # данные для сайта
python3 иконки.py       # иконки приложения
cd web && npm run build # сайт и приложение
```

## Проверить

```bash
python3 проверка.py
```

Разбирает колонку 10-Л из заводского файла заново — по рамкам ячеек — и
сверяет с `data.py`. Заодно проверяет звонки (сорок пять минут, без наложений),
целостность блоков, число уроков, наличие эпиграфов и текстов, а в собранных
PDF — число полос, разделы, подпись студии и то, что ни одно слово не выходит
за поля. Ноль на выходе — всё сошлось.

Проверка полей требует `pdfplumber`; без него она пропускается с замечанием.

## Выложить

Ветка `gh-pages` содержит только сборку, исходники живут в `main`.
Всё обновляется одной командой:

```bash
./деплой.sh
```

Скрипт пересобирает три PDF, экспортирует данные, собирает сайт и жёстко
переписывает `gh-pages`. Pages включились сами при первом пуше этой ветки.

Сайт закрыт от индексации: `noindex` в `<head>` и `robots.txt`.

## Что откуда

| Файл | Что делает |
|---|---|
| `книга.py` | вёрстка двенадцати полос и примитивы: разрядка, волосяная линия, спектр |
| `книга-крафт.py` | то же по крафту: земляные краски, штамп, этикетка, клетки элементов |
| `таблица.py` | вся неделя одной таблицей: ни цвета, ни заливок — только рамки и вес шрифта |
| `крафт.py` | генератор бумаги: зерно, волокна, крапины, выцветание к краям |
| `data.py` | содержание: звонки, блоки уроков, палитра, эпиграфы дней, общие тексты |
| `marks.py` | знаки: кубы СУНЦ и печатка 10-Л, в reportlab и в SVG |
| `экспорт.py` | данные книг → `web/src/data.json` |
| `проверка.py` | сверка с источником и проверка внутренней логики |
| `web/` | React + Vite, две страницы: витрина и карманное расписание (PWA) |
| `иконки.py` | иконки приложения из геометрии знака: reportlab → PDF → PNG |
| `fonts/` | Unbounded, Manrope, JetBrains Mono — статические начертания под кириллицу |
| `source-1-semestr-26-27-s-kabinetami_s-03-09.xlsx` | заводской файл с сайта интерната |

## Откуда данные

`internat.msu.ru` → «Учащимся» → «Расписание уроков»:
файл `source-1-semestr-26-27.xlsx`, колонка **10-Л** (каб. 40).
Границы сдвоенных блоков сняты по рамкам ячеек и **сверены с картинкой**
`raspisanie-26-27-1-sem-2-scaled.jpg` с той же страницы. Звонки —
со страницы «Учебный отдел», единые для всех классов.

Расхождение источника оставлено как есть: полдник в нём начинается в 16:40,
за пять минут до конца 8-го урока. Об этом сказано на полосе «Звонки».

Химпрактикум в среду по сетке занимает 7-й и 8-й уроки и тянется дальше —
в исходнике его блок доходит до десятой позиции, для которой звонков нет.

## Направление

`ФИЛОСОФИЯ.md` — «Хронохром»: время откладывается в подлинной пропорции,
за каждым смыслом закреплён свой цвет. Высота полосы равна длительности,
провал в левой колонке блока — перемена внутри сдвоенного урока.

Знак класса — печатка: ароматическое кольцо, шесть рёбер шести цветов, внутри
номер класса. Химия для химического класса и одновременно шесть учебных дней.
Эмблема СУНЦ перерисована вектором с `logo.png` сайта, цвета сняты пипеткой.

Крафтовое издание — тот же порядок полос в мире лабораторного журнала:
ризография земляными красками по обёрточной бумаге, наклеенная этикетка
реактива, оттиск штампа, следы стакана, предметы клетками как элементы.

## Грабли

- **Переменные шрифты** reportlab берёт в дефолтном начертании: Unbounded,
  Manrope и Onest пришлось нарезать на статические через `fontTools.instancer`.
- **Разрядку** нельзя ставить через `Tc` — она утекает в весь дальнейший поток.
  В `книга.py` каждый знак сдвигается руками (`разрядка`).
- **Ось Y в SVG** идёт вниз, а в reportlab вверх. В `svg_sunc` из-за этого
  третий (синий) куб сначала уезжал за нижний край viewBox: в вебе знак был из
  двух кубов, в книге — из трёх. Верхний ряд кубов сидит на `r`, а не на `2.5r`.
- **Структура сдвоенных уроков** в xlsx не задана объединением ячеек: слияния
  там только горизонтальные, для потоков между классами. Границы блока читаются
  по нижней рамке (`thin`) — по ней и разобрана колонка.

## Новый семестр или другой класс

1. Скачать свежий файл со страницы «Расписание уроков» интерната и положить
   его сюда как `source-1-semestr-26-27.xlsx` (имя прописано в `проверка.py`).
2. Разобрать нужную колонку **по рамкам ячеек**: границей блока служит либо
   нижняя рамка строки, либо верхняя рамка следующей — в исходнике встречаются
   оба способа. Слияния там только горизонтальные, между классами.
3. Поправить `data.py`: `WEEK`, при необходимости `LESSON_TIMES`, `ЭПИГРАФ`
   (по одной строке на день, про то, что в этот день действительно стоит) и
   `ТЕКСТЫ`.
4. `python3 проверка.py` — она сверит `data.py` с файлом и не даст разойтись.
5. `./деплой.sh` — пересоберёт три издания, сайт и приложение и выложит.

## Что осталось неcделанным

Зафиксировано на момент закрытия, чтобы не искать заново:

- **Доступности не касались.** Нет `aria-current` на выбранном дне, карточка
  «сейчас» обновляется каждые пятнадцать секунд без живой области, порядок
  фокуса и клавиатурный проход не проверены.
- **Нет тестов на `nowState`** — самую хрупкую логику (границы уроков,
  перемены, обед, воскресенье) проверяли только глазами в один момент времени.
- **Офлайн не проверен.** Service worker написан и регистрируется, но что
  приложение открывается без сети, никто не убедился.
- **Вес.** Шрифты обоих миров грузятся сразу — двенадцать файлов, хотя
  одновременно нужен один. `kraft.jpg` весит 787 КБ; в системе есть `cwebp`.
- **Панель браузера** не работает с этим проектом, пока сессия запущена не из
  его папки: проверять приходится через `chrome-headless-shell` вслепую.
